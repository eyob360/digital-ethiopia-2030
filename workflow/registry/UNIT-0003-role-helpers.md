---
id: UNIT-0003
name: Role helpers
kind: util
path: src/lib/auth/roles.ts
status: active
---

# UNIT-0003: Role helpers

**Purpose:** Shared role constants, guards, and authorization predicates for `OPERATOR` and `VIEWER`.

**Interface:** `userRoles`, `canViewDashboard(role)`, `canUseOperatorControls(role)`, and `assertKnownRole(role)`.

**Variants/options:** None.
